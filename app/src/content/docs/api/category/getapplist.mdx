---
title: search
description: Метод позволяет получить информацию о списке приложений, находящихся в определенной категории приложений.
---

import { Aside, Tabs, TabItem, Badge, Card, Icon } from '@astrojs/starlight/components';

<style>{`
    h1#_top {
        display: none !important;
    }
    
    .sl-markdown-content,
    .content-panel {
        border-top: none !important;
        border-bottom: none !important;
    }
    
    .sl-markdown-content > h1:first-of-type {
        margin-top: 0 !important;
        padding-top: 0 !important;
    }
    
    .sl-markdown-content {
        padding-top: 0 !important;
    }
    
    .content-panel {
        padding-top: 0.0rem !important;
    }
`}</style>

# <Badge text="GET" variant="note" size="medium" /> `/method/marketplace/search`

Метод позволяет получить информацию о списке приложений, находящихся в определенной категории приложений.

---

### <Icon name="open-book" color="var(--sl-color-accent)" /> Параметры запроса (Query)

| Параметр | Тип | Обязательно | Описание |
| :--- | :--- | :--- | :--- |
| `id` | `string` | **Да** | ID категории :-3 |

---

### <Icon name="rocket" color="var(--sl-color-accent)" /> Пример использования

<Tabs>
  <TabItem label="cURL" icon="terminal">
    ```bash
    curl -X 'GET' \
      'https://api.lunastore.app/method/category/getAppList?id=1' \
      -H 'accept: application/json'
    ```
  </TabItem>
  <TabItem label="Python" icon="seti:python">
    ```python
    import requests

    params = {
        'id': 1,
    }

    response = requests.get('https://api.lunastore.app/method/category/getAppList', params=params)
    print(response.json())
    ```
  </TabItem>
  <TabItem label="Go" icon="seti:go">
    ```go
    resp, err := http.Get("https://api.lunastore.app/method/category/getAppList?id=1")
    ```
  </TabItem>
  <TabItem label="C#" icon="seti:c-sharp">
    ```csharp
    using System;
    using System.Net.Http;
    using System.Threading.Tasks;

    class Program
    {
        static async Task Main()
        {
            using var client = new HttpClient();
            var url = "https://api.lunastore.app/method/category/getAppList?id=1";
            
            try
            {
                string response = await client.GetStringAsync(url);
                Console.WriteLine(response);
            }
            catch (HttpRequestException e)
            {
                Console.WriteLine($"Error of request: {e.Message}");
            }
        }
    }
    ```
  </TabItem>
  <TabItem label="C++" icon="seti:cpp">
    ```cpp
    #include <iostream>
    #include <string>
    #include <curl/curl.h>

    // callback for write answer in string
    size_t WriteCallback(void* contents, size_t size, size_t nmemb, std::string* userp) {
        userp->append((char*)contents, size * nmemb);
        return size * nmemb;
    }

    int main() {
        CURL* curl = curl_easy_init();
        if(curl) {
            std::string readBuffer;
            curl_easy_setopt(curl, CURLOPT_URL, "https://api.lunastore.app/method/category/getAppList?id=1");
            curl_easy_setopt(curl, CURLOPT_WRITEFUNCTION, WriteCallback);
            curl_easy_setopt(curl, CURLOPT_WRITEDATA, &readBuffer);

            CURLcode res = curl_easy_perform(curl);
            if(res == CURLE_OK) {
                std::cout << readBuffer << std::endl;
            } else {
                std::cerr << "cURL error: " << curl_easy_strerror(res) << std::endl;
            }
            curl_easy_cleanup(curl);
        }
        return 0;
    }
    ```
  </TabItem>
</Tabs>

---

### <Icon name="comment-alt" color="var(--sl-color-accent)" /> Формат ответа

При успешном запросе возвращается список, внутри которого находятся объекты `application`.

```json 
{
  "1": {
    "id": 4,
    "title": "Test App 1",
    "description": "Test Description",
    "original_author": "Daniel Myslivets",
    "slogan": "Test Slogan",
    "price": 0,
    "requirements": "fucking computer",
    "screenshot_urls": [],
    "developer_site": "https://fayzetw.in",
    "is_demo": false,
    "is_under_dmca": false,
    "icon_url": "//192.168.48.128:8080/public/f4/55/f455926b139739d0f051279641b3b3b422924584159e526548810fa95010c0de.png"
  },
}
