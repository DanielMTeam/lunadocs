---
title: Инструкция по установке
description: Пошаговое руководство по развертыванию LunaStore.
tableOfContents: false
---

import { Aside, Steps, Tabs, TabItem, Card, Icon } from '@astrojs/starlight/components';

<Aside type="caution" title="Системные требования">
  Перед тем, как начать процесс установки на сервер, убедитесь, что он соответствует минимальным характеристикам LunaStore:
  - **Процессор:** 2 ядра и более
  - **ОЗУ:** 4 ГБ и более
  - **Диск:** 20+ ГБ свободного места
  - **Установленное ПО:** Docker, Docker Compose, Python 3.12+, PIP
</Aside>

Выберите удобный способ развертывания:

<Tabs>
<TabItem label="Автоматическая установка" icon="rocket">
    
    <Aside type="tip" title="Запуск скрипта">
      Выберите вашу операционную систему ниже, чтобы увидеть команды для терминала:
    </Aside>

    <Tabs>
      <TabItem label="Linux (.sh)">
        ```bash
        chmod +x setup.sh && ./setup.sh
        ```
        *Используйте `bash setup.sh`, если нет прав на выполнение.*
      </TabItem>
      <TabItem label="Windows (.ps1)">
        ```powershell
        .\setup.ps1
        ```
      </TabItem>
    </Tabs>
  </TabItem>
  
  <TabItem label="Ручная установка" icon="setting">
    <Steps>
    1.  ### Склонируйте репозиторий на сервер
        ```bash
        git clone https://git.myslivets.com/myslivets/lunastore
        cd lunastore
        ```

    2.  ### Установите зависимости из файла в репозитории
        ```bash
        pip install -r requirements.txt
        ```

    3.  ### Настройте первоначальное окружение вашего будущего LunaStore-сервера
        ```bash
        cp .env.example .env
        ```
        *(Отредактируйте .env, заполнив параметры БД и OpenID)*

    4.  ### Сделайте первичную подготовку базы данных
        <Tabs>
          <TabItem label="Production">
            ```bash
            make migrate
            make cachetable
            ```
          </TabItem>
          <TabItem label="Development">
            ```bash
            make dev-migrate
            make dev-cachetable
            ```
          </TabItem>
        </Tabs>

    5.  ### Создайте свой первый аккаунт (обязательно с правами администратора)
        ```bash
        make superuser
        ```

    6.  ### Соберите воедино и наслаждайтесь
        ```bash
        make build
        make up
        ```
    </Steps>
  </TabItem>
</Tabs>

---

## <Icon name="information" color="var(--sl-color-accent)" /> Архитектура и порты

LunaStore распределен по трем основным контейнерам. Убедитесь, что данные порты открыты в вашем фаерволе:

| Контейнер | Назначение | Порт |
| :--- | :--- | :--- |
| **lunaStore** | Основной сайт и контент | `9088` |
| **lunaStoreAdmin** | Панель управления | `8088` |
| **lunaStoreAPI** | Функционал API | `7088` |

<Aside type="note">
  Если вам нужно изменить порты, отредактируйте файл `docker-compose.yml` перед запуском команды `make build`.
</Aside>

<div style="margin-top: 3rem; text-align: center;">
  **Нужна помощь?** Загляните в наш [Telegram-канал](https://t.me/lunastore_official)
</div>
